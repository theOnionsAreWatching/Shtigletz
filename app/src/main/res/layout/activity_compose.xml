<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView android:id="@+id/composeTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            android:singleLine="true" android:ellipsize="end"
            android:paddingBottom="6dp" />

        <EditText android:id="@+id/composeTo"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="@string/hint_to" android:inputType="textEmailAddress" />

        <EditText android:id="@+id/composeCc"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="@string/hint_cc" android:inputType="textEmailAddress" />

        <EditText android:id="@+id/composeSubject"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="@string/hint_subject" android:inputType="text" />

        <EditText android:id="@+id/composeBody"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="@string/hint_body"
            android:inputType="textMultiLine|textCapSentences"
            android:gravity="top"
            android:minLines="8" />

        <TextView android:id="@+id/attachedLabel"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textSize="12sp"
            android:focusable="true" android:clickable="true"
            android:background="@drawable/focus_bg" android:padding="6dp"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:layout_marginTop="8dp">
            <Button android:id="@+id/attachButton" style="@style/DMail.Button"
                android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1"
                android:layout_marginEnd="4dp" android:visibility="gone"
                android:text="@string/compose_attach" />
            <Button android:id="@+id/sendButton" style="@style/DMail.Button"
                android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="2"
                android:layout_marginStart="4dp"
                android:text="@string/compose_send" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
