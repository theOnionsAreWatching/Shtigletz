package io.github.theonionsarewatching.shtigletz

/** D-Mail Plus: attachments can be saved/opened/sent. Rendering stays
 *  identical to Kosher — no images, embedded or otherwise. */
object FlavorConfig {
    const val NAME = "plus"
    const val ATTACHMENTS = true
    const val IMAGES = false
}
